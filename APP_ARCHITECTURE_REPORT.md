# EatSense Mobile App - Полный отчёт по архитектуре и проблемам

## 📱 О приложении

**EatSense** — мобильное приложение для отслеживания питания с AI-помощником. React Native + Expo SDK 54, TypeScript/JavaScript, NestJS backend на Railway.

**Технологический стек:**
- React Native 0.81.5 + React 19.1.0
- Expo SDK 54.0.0
- React Navigation 7.x (Stack + Bottom Tabs)
- i18next для интернационализации
- Moti для анимаций
- Expo Camera, Image Picker для работы с фото
- NestJS API на Railway (PostgreSQL, Prisma, Redis)

---

## 🏗️ Архитектура приложения

### 1. **Навигация и структура экранов**

**Текущая структура (`App.js`):**

```
App (Root)
├── ErrorBoundary
├── I18nProvider
└── AppWrapper
    └── AppContent
        └── NavigationContainer
            ├── (if !authenticated)
            │   ├── _SmokeTest (начальный экран для отладки)
            │   └── Auth (экран авторизации)
            │
            └── (if authenticated)
                └── Stack Navigator
                    └── MainTabs
                        └── Stack Navigator (❌ ПРОБЛЕМА: должен быть Bottom Tab Navigator)
                            └── Dashboard (единственный экран)
```

**Проблема:** `MainTabs` — это `Stack.Navigator` с одним экраном `Dashboard`, а должен быть `createBottomTabNavigator` с 4 табами:
1. **Dashboard** — главный экран с калориями и статистикой
2. **Articles** — статьи о питании
3. **Recently** — недавно добавленные блюда
4. **Profile** — профиль пользователя

**Файлы навигации:**
- `App.js` — корневая навигация (Stack)
- `MainTabs` — сейчас неправильно реализован (Stack вместо Bottom Tabs)
- Должен быть: `src/navigation/MainTabsNavigator.tsx` или в `App.js`

---

### 2. **Экраны приложения**

#### ✅ **Реализованные экраны:**

1. **DashboardScreen** (`src/screens/DashboardScreen.js`)
   - Главный экран после авторизации
   - Показывает калории, макросы (белки, жиры, углеводы)
   - Календарь для выбора даты
   - Статистика за месяц
   - Статьи (featured + feed) — **проблема: занимают много места**
   - Кнопка "+" (FAB) для анализа фото
   - AI Assistant кнопка
   - Недавно добавленные блюда

2. **AuthScreen** (`src/components/AuthScreen.js`)
   - Экран авторизации
   - Поддерживает: Email OTP, Google OAuth, Apple Sign In
   - После успешной авторизации навигирует на `Onboarding` или `MainTabs`

3. **OnboardingScreen** (`src/screens/OnboardingScreen.js`)
   - Многошаговый онбординг (6 шагов)
   - Сбор данных: имя, возраст, рост, вес, пол, уровень активности, цели, план подписки
   - Прогресс-бар, горизонтальный скролл

4. **ArticlesScreen** (`src/screens/ArticlesScreen.js`)
   - Лента статей
   - Поиск, фильтры по тегам
   - Featured articles вверху

5. **RecentlyScreen** (`src/screens/RecentlyScreen.js`)
   - Недавно добавленные блюда
   - Pull-to-refresh
   - Отображение калорий, времени

6. **ProfileScreen** (`src/screens/ProfileScreen.js`)
   - Профиль пользователя
   - Настройки, персональные данные

7. **CameraScreen** (`src/screens/CameraScreen.js`)
   - Полноэкранная камера
   - Снимок фото → навигация на `AnalysisResults`
   - Реализован, но **проблема: не регистрирован в навигации**

8. **GalleryScreen** (`src/screens/GalleryScreen.js`)
   - Галерея для выбора фото
   - → навигация на `AnalysisResults`
   - Реализован, но **проблема: не регистрирован в навигации**

9. **AnalysisResultsScreen** (`src/screens/AnalysisResultsScreen.js`)
   - Результаты анализа фото
   - Показ калорий, макросов
   - Реализован, но **проблема: не регистрирован в навигации**

10. **ArticleDetailScreen** (`src/screens/ArticleDetailScreen.js`)
    - Детальная страница статьи
    - Реализован, но **проблема: не регистрирован в навигации**

11. **SmokeTestScreen** (`src/screens/SmokeTestScreen.js`)
    - Временный экран для отладки
    - Кнопки "Ping API", "Перейти в приложение"
    - Можно убрать после фикса всех проблем

#### ❌ **Не реализованные / проблемы:**

- Bottom Tab Navigator — отсутствует
- Все экраны (Camera, Gallery, AnalysisResults, ArticleDetail) не зарегистрированы в `NavigationContainer`

---

### 3. **Компоненты**

#### ✅ **Реализованные:**

1. **AiAssistant** (`src/components/AiAssistant.tsx`)
   - ❌ **ПРОБЛЕМА:** Сейчас это stub (заглушка) с текстом "AI-ассистент скоро будет доступен"
   - Реальная реализация должна быть в `RealAiAssistant.tsx` (если есть)

2. **HealthScoreCard** (`src/components/HealthScoreCard.tsx`)
   - Карточка Health Score для блюд

3. **ProfileModal** (`src/components/ProfileModal.tsx`)
   - Модальное окно профиля

4. **AnalysisFlow** (`src/components/AnalysisFlow.tsx`)
   - Компонент для анализа фото (камера/галерея)

5. **ImagePickerBlock** (`src/components/ImagePickerBlock.tsx`)
   - Блок выбора изображения

6. **ErrorBoundary** (`src/components/ErrorBoundary.tsx`)
   - Обработка ошибок React

#### ❌ **Проблемы компонентов:**

- `AiAssistant` — не работает (stub)
- Компоненты камеры/галереи есть, но навигация к ним не работает

---

### 4. **Контексты (State Management)**

#### ✅ **Реализованные:**

1. **AuthContext** (`src/contexts/AuthContext.tsx`)
   - Управляет состоянием пользователя
   - `user`, `loading`, `refreshUser()`, `signOut()`
   - Использует `ApiService.getUserProfile()`

2. **ThemeContext** (`src/contexts/ThemeContext.js`)
   - Тема приложения (light/dark)
   - Цвета, токены дизайн-системы

#### ✅ **Работает корректно**

---

### 5. **Сервисы**

1. **ApiService** (`src/services/apiService.js`)
   - HTTP-клиент для работы с API
   - Методы: `getUserProfile()`, `getMeals()`, `getArticles()`, `analyzeImage()`, `verifyOtp()`, и т.д.
   - Хранение токенов в SecureStore

2. **i18n** (`app/i18n/`)
   - Интернационализация (en, es, de, fr, ko, ja, zh)
   - `useI18n()` hook для переводов

---

### 6. **Backend API**

**Endpoints (Railway):**
- `POST /auth/magic-link` — отправка OTP
- `POST /auth/verify-otp` — проверка OTP
- `POST /auth/google` — Google OAuth
- `POST /auth/apple` — Apple Sign In
- `GET /api/users/me` — профиль пользователя
- `GET /api/meals` — список блюд
- `POST /api/food/analyze` — анализ фото
- `GET /api/articles` — статьи
- `GET /api/articles/:slug` — детальная статья
- `GET /api/stats/daily` — дневная статистика
- `GET /api/stats/monthly` — месячная статистика

---

## 🐛 Текущие проблемы (критические)

### **1. Нет Bottom Tab Navigator ❌**

**Что должно быть:**
```jsx
import { createBottomTabNavigator } from '@react-navigation/bottom-tabs';

const Tab = createBottomTabNavigator();

function MainTabs() {
  return (
    <Tab.Navigator>
      <Tab.Screen name="Dashboard" component={DashboardScreen} />
      <Tab.Screen name="Articles" component={ArticlesScreen} />
      <Tab.Screen name="Recently" component={RecentlyScreen} />
      <Tab.Screen name="Profile" component={ProfileScreen} />
    </Tab.Navigator>
  );
}
```

**Что есть сейчас:**
```jsx
function MainTabs() {
  return (
    <Stack.Navigator>
      <Stack.Screen name="Dashboard" component={DashboardScreen} />
    </Stack.Navigator>
  );
}
```

**Последствия:**
- Нет нижней панели навигации
- Нельзя переключиться между экранами
- Все остальные экраны недоступны

---

### **2. Экран камеры не открывается ❌**

**Проблема:**
- `DashboardScreen` пытается навигировать на `Camera`: `navigation.navigate('Camera')`
- Но экран `Camera` не зарегистрирован в навигации

**Что нужно:**
- Зарегистрировать `CameraScreen` в Stack Navigator внутри `MainTabs`
- Или создать отдельный Stack для модальных экранов

---

### **3. Галерея не открывается ❌**

**Проблема:**
- `DashboardScreen` пытается навигировать на `Gallery`: `navigation.navigate('Gallery')`
- Но экран `Gallery` не зарегистрирован в навигации

**Что нужно:**
- Зарегистрировать `GalleryScreen` в Stack Navigator

---

### **4. AI Assistant не работает ❌**

**Проблема:**
- `AiAssistant.tsx` — это stub с текстом "AI-ассистент скоро будет доступен"
- Нет реальной функциональности

**Что нужно:**
- Восстановить реальную реализацию из `RealAiAssistant.tsx` (если есть)
- Или создать новую реализацию с чатом

---

### **5. Статьи занимают много места на Dashboard ❌**

**Проблема:**
- На `DashboardScreen` отображаются featured articles + feed articles
- Это занимает много вертикального пространства
- Статьи должны быть в отдельном табе `Articles`

**Что нужно:**
- Убрать или сократить секцию статей на Dashboard
- Показать только 2-3 featured статьи с ссылкой "Смотреть всё" → `ArticlesScreen`

---

### **6. Ничего не кликается / не работает ❌**

**Возможные причины:**
1. **TouchableOpacity/Button не работает:**
   - Проверить, не перекрывает ли что-то элементы
   - Проверить `pointerEvents` в стилях
   - Проверить `zIndex`

2. **Навигация не работает:**
   - Экраны не зарегистрированы в `NavigationContainer`
   - Неправильные имена маршрутов

3. **Модальные окна не открываются:**
   - Проверить `Modal` visibility state
   - Проверить `onPress` handlers

**Что нужно проверить:**
- Консоль на наличие ошибок
- `clientLog` логи в Railway
- React DevTools для проверки состояния

---

### **7. Онбординг может не работать ❌**

**Проблема:**
- После авторизации должна быть проверка: `user.isOnboardingCompleted`
- Если `false` → показать `OnboardingScreen`
- Если `true` → показать `MainTabs`

**Что нужно проверить:**
- Логика в `AuthScreen` после успешной авторизации
- Сохранение `isOnboardingCompleted` в backend
- Навигация после завершения онбординга

---

### **8. Экраны не зарегистрированы в навигации ❌**

**Экраны, которые нужно зарегистрировать:**
- `CameraScreen`
- `GalleryScreen`
- `AnalysisResultsScreen`
- `ArticleDetailScreen`
- `ProfileScreen`
- `ArticlesScreen`
- `RecentlyScreen`
- `OnboardingScreen` (уже есть, но проверить логику)

---

## ✅ Что работает

1. ✅ Авторизация (Email OTP, Google, Apple)
2. ✅ API подключение
3. ✅ Загрузка данных (статистика, статьи, блюда)
4. ✅ i18n (переводы)
5. ✅ Тема (light/dark)
6. ✅ Базовый UI отображается

---

## 🔧 План исправлений (приоритет)

### **Приоритет 1: Критические (блокируют использование)**

1. **Создать Bottom Tab Navigator**
   - Заменить `MainTabs` на `createBottomTabNavigator`
   - Добавить 4 таба: Dashboard, Articles, Recently, Profile
   - Настроить иконки и стили

2. **Зарегистрировать все экраны в навигации**
   - Camera, Gallery, AnalysisResults, ArticleDetail
   - Добавить в Stack Navigator внутри табов

3. **Исправить навигацию камеры/галереи**
   - Убедиться, что кнопки "+" на Dashboard работают
   - Модальное окно выбора (Camera/Gallery) открывается

### **Приоритет 2: Важные (влияют на UX)**

4. **Убрать/сократить статьи на Dashboard**
   - Показать 2-3 featured статьи
   - Добавить ссылку "Смотреть всё" → ArticlesScreen

5. **Восстановить AI Assistant**
   - Заменить stub на реальную реализацию
   - Или временно скрыть, если не готово

6. **Исправить кликабельность элементов**
   - Проверить TouchableOpacity
   - Проверить перекрытия элементов
   - Добавить логгирование для отладки

### **Приоритет 3: Улучшения**

7. **Проверить онбординг**
   - Убедиться, что логика перехода работает
   - Сохраняется `isOnboardingCompleted`

8. **Убрать SmokeTestScreen**
   - После фикса всех проблем убрать из навигации
   - Или оставить для отладки

---

## 📝 Детали реализации

### **Как должна работать навигация:**

```jsx
// App.js
<NavigationContainer>
  {isAuthenticated ? (
    <Stack.Navigator>
      {/* Модальные экраны */}
      <Stack.Screen name="Onboarding" component={OnboardingScreen} />
      
      {/* Основной флоу с табами */}
      <Stack.Screen name="MainTabs" component={MainTabs} options={{ headerShown: false }} />
      
      {/* Детальные экраны (модальные) */}
      <Stack.Screen name="ArticleDetail" component={ArticleDetailScreen} />
      <Stack.Screen name="AnalysisResults" component={AnalysisResultsScreen} />
    </Stack.Navigator>
  ) : (
    <Stack.Navigator>
      <Stack.Screen name="Auth" component={AuthScreen} />
    </Stack.Navigator>
  )}
</NavigationContainer>

// MainTabsNavigator.tsx (новый файл)
const Tab = createBottomTabNavigator();

export function MainTabs() {
  return (
    <Tab.Navigator>
      <Tab.Screen 
        name="Dashboard" 
        component={DashboardScreen}
        options={{
          tabBarIcon: ({ color }) => <Ionicons name="home" size={24} color={color} />,
          tabBarLabel: 'Dashboard',
        }}
      />
      <Tab.Screen 
        name="Articles" 
        component={ArticlesScreen}
        options={{
          tabBarIcon: ({ color }) => <Ionicons name="newspaper" size={24} color={color} />,
          tabBarLabel: 'Articles',
        }}
      />
      <Tab.Screen 
        name="Recently" 
        component={RecentlyScreen}
        options={{
          tabBarIcon: ({ color }) => <Ionicons name="time" size={24} color={color} />,
          tabBarLabel: 'Recently',
        }}
      />
      <Tab.Screen 
        name="Profile" 
        component={ProfileScreen}
        options={{
          tabBarIcon: ({ color }) => <Ionicons name="person" size={24} color={color} />,
          tabBarLabel: 'Profile',
        }}
      />
    </Tab.Navigator>
  );
}
```

### **Навигация внутри табов (Stack внутри каждого таба):**

```jsx
// Для таба Dashboard
<Tab.Screen name="Dashboard">
  {() => (
    <Stack.Navigator>
      <Stack.Screen name="DashboardMain" component={DashboardScreen} />
      <Stack.Screen name="Camera" component={CameraScreen} />
      <Stack.Screen name="Gallery" component={GalleryScreen} />
      <Stack.Screen name="AnalysisResults" component={AnalysisResultsScreen} />
    </Stack.Navigator>
  )}
</Tab.Screen>
```

---

## 🎯 Критерии готовности

Приложение считается готовым, когда:

1. ✅ Есть нижняя панель навигации с 4 табами
2. ✅ Все экраны доступны и работают
3. ✅ Камера и галерея открываются и работают
4. ✅ AI Assistant работает или скрыт
5. ✅ Статьи не занимают весь Dashboard
6. ✅ Все кнопки кликабельны
7. ✅ Онбординг работает корректно
8. ✅ Нет белых экранов и крашей

---

## 📦 Зависимости (важные)

```json
{
  "@react-navigation/bottom-tabs": "7.8.5",
  "@react-navigation/native": "7.1.21",
  "@react-navigation/stack": "7.6.4",
  "expo-camera": "~17.0.9",
  "expo-image-picker": "~17.0.8",
  "react-native-reanimated": "~4.1.1",
  "react-native-gesture-handler": "~2.28.0"
}
```

Все установлены, нужно только правильно использовать.

---

## 🔍 Отладка

**Логи:**
- `clientLog()` отправляет логи на `/debug/client-log` в backend
- Логи можно смотреть в Railway logs

**Консоль:**
- Metro bundler показывает JS-ошибки
- React Native Debugger для инспекции состояния

**Проверка навигации:**
- `navigation.getState()` — текущее состояние навигации
- `navigation.canGoBack()` — можно ли вернуться назад

---

## 📚 Структура файлов (ключевые)

```
src/
├── screens/
│   ├── DashboardScreen.js ✅
│   ├── ArticlesScreen.js ✅
│   ├── RecentlyScreen.js ✅
│   ├── ProfileScreen.js ✅
│   ├── CameraScreen.js ✅ (не зарегистрирован)
│   ├── GalleryScreen.js ✅ (не зарегистрирован)
│   ├── AnalysisResultsScreen.js ✅ (не зарегистрирован)
│   ├── ArticleDetailScreen.js ✅ (не зарегистрирован)
│   └── OnboardingScreen.js ✅
├── components/
│   ├── AiAssistant.tsx ❌ (stub)
│   ├── AuthScreen.js ✅
│   └── ...
├── contexts/
│   ├── AuthContext.tsx ✅
│   └── ThemeContext.js ✅
├── services/
│   └── apiService.js ✅
└── navigation/ (отсутствует — нужно создать)
    └── MainTabsNavigator.tsx ❌ (нужно создать)

App.js — корневой компонент
```

---

**Конец отчёта.**

