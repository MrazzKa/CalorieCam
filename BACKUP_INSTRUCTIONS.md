# Инструкция по созданию бэкапа приложения EatSense

## 🚀 Быстрый способ (автоматический скрипт)

### 1. Запустите скрипт бэкапа:

```bash
cd /home/mrazzka/projects/work/caloriecam
./backup.sh
```

Скрипт автоматически:
- ✅ Проверит git статус
- ✅ Создаст коммит со всеми изменениями (если есть)
- ✅ Запушит изменения в удалённый репозиторий (если настроен)
- ✅ Создаст архив проекта (без node_modules и временных файлов)
- ✅ Сохранит бэкап в `~/backups/eatsense/`

---

## 📦 Ручной способ

### Вариант 1: Git commit + push

```bash
# Перейти в директорию проекта
cd /home/mrazzka/projects/work/caloriecam

# Проверить статус
git status

# Добавить все изменения
git add -A

# Создать коммит
git commit -m "backup: snapshot $(date +%Y-%m-%d_%H:%M:%S)"

# Запушить в удалённый репозиторий
git push
```

### Вариант 2: Создать архив проекта

```bash
# Перейти в директорию проекта
cd /home/mrazzka/projects/work/caloriecam

# Создать директорию для бэкапов (если её нет)
mkdir -p ~/backups/eatsense

# Создать архив (без node_modules и временных файлов)
tar -czf ~/backups/eatsense/eatsense_backup_$(date +%Y%m%d_%H%M%S).tar.gz \
  --exclude=node_modules \
  --exclude=.expo \
  --exclude=dist \
  --exclude=build \
  --exclude=coverage \
  --exclude="*.log" \
  --exclude=".DS_Store" \
  --exclude=".cache" \
  --exclude=".pnpm-store" \
  .

echo "✅ Backup created in ~/backups/eatsense/"
```

### Вариант 3: Копирование в другую директорию

```bash
# Создать директорию для бэкапа
mkdir -p ~/backups/eatsense/$(date +%Y%m%d_%H%M%S)

# Скопировать проект (без node_modules)
rsync -av --exclude 'node_modules' \
           --exclude '.expo' \
           --exclude 'dist' \
           --exclude 'build' \
           --exclude 'coverage' \
           --exclude '*.log' \
           /home/mrazzka/projects/work/caloriecam/ \
           ~/backups/eatsense/$(date +%Y%m%d_%H%M%S)/
```

---

## 📋 Что включает бэкап

### ✅ Включается:
- Весь исходный код (`src/`, `apps/`)
- Конфигурационные файлы (`package.json`, `app.config.js`, `eas.json`, и т.д.)
- Документация (`README.md`, `APP_ARCHITECTURE_REPORT.md`, и т.д.)
- Git история (если используется git)
- Environment файлы (если не в .gitignore)

### ❌ Исключается:
- `node_modules/` — зависимости (восстанавливаются через `pnpm install`)
- `.expo/` — временные файлы Expo
- `dist/`, `build/` — собранные файлы
- `coverage/` — отчёты о покрытии тестами
- `*.log` — логи
- `.cache/`, `.pnpm-store/` — кеши

---

## 🔄 Восстановление из бэкапа

### Из архива:

```bash
# Распаковать архив
tar -xzf ~/backups/eatsense/eatsense_backup_YYYYMMDD_HHMMSS.tar.gz -C /path/to/restore/location

# Перейти в восстановленную директорию
cd /path/to/restore/location

# Установить зависимости
pnpm install

# Настроить environment (если нужно)
cp .env.example .env
# Отредактировать .env с вашими значениями
```

### Из git:

```bash
# Клонировать репозиторий
git clone <repository-url> /path/to/restore/location

# Или перейти к нужному коммиту
cd /path/to/project
git checkout <commit-hash>

# Установить зависимости
pnpm install
```

---

## 💡 Рекомендации

1. **Регулярные бэкапы**: Создавайте бэкапы перед важными изменениями
2. **Множественные копии**: Храните бэкапы в разных местах (локально, в облаке)
3. **Версионирование**: Используйте git для отслеживания изменений
4. **Тестирование**: Периодически проверяйте, что бэкапы можно восстановить

---

## 📍 Расположение бэкапов

По умолчанию бэкапы сохраняются в:
```
~/backups/eatsense/
```

Чтобы изменить расположение, отредактируйте переменную `BACKUP_DIR` в скрипте `backup.sh`.

---

## 🔐 Безопасность

- **Не коммитьте** чувствительные данные (API ключи, пароли) в git
- Используйте `.env` файлы для конфигурации
- Добавьте `.env` в `.gitignore`
- Храните бэкапы в зашифрованном виде, если они содержат чувствительные данные

---

## ❓ Вопросы и проблемы

Если скрипт бэкапа не работает:

1. Проверьте права на выполнение: `chmod +x backup.sh`
2. Проверьте, что вы в правильной директории
3. Убедитесь, что есть место на диске
4. Проверьте логи ошибок в консоли

